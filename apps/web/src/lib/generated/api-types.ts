/**
 * Placeholder for generated OpenAPI types.
 * Run `pnpm --filter @fin-nest/web generate:api` while the API is available.
 */
export type paths = Record<string, never>;
export type components = Record<string, never>;
